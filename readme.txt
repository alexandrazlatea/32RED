Steps to install the project:
 
  git pull https://github.com/alexandrazlatea/32RED
  install node js (https://nodejs.org/en/)
  run npm install -g bower
  go to project folder
  run bower install

Open the browser and type http://localhost/32red/#/promotion 
